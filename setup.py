import setuptools

setuptools.setup(
    name="noisy_farm",
    version="0.0.1",
    description="A toy project to recognize farm animals",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
)